# 決定事項の記録

このファイルはプロジェクトの確定事項の唯一の記録。これに反する提案・実装はしない。
未決事項は末尾の「検討中」に列挙する。

## 確定済み

### 2026-08-12: プロジェクトの基本方針（ユーザー要件）

- レトロゲーム Bi-Planes のブラウザ版リメイク。本家のシンプルな面白さは引き継ぎ、グラフィックとルールに手を加える
- 2人対戦。二機の飛行機を操縦し、相手を撃墜して得点を競う。規定点数に達したら勝ち
- リアルな操作性（急上昇での失速など）を実現する
- 操作: 上下 + 弾発射（本家準拠）に加え、スロットル操作と2種類の弾（7.7mm / 20mm）
- 耐久力制: 7.7mm は複数発、20mm は一撃で撃墜
- 被弾が増えるとエンジンパワーか旋回力が鈍る。被弾時は煙の演出
- グラフィックはカップヘッド風レトロカトゥーン
- 1画面固定、スクロールなし
- 気球がランダムで上昇し、撃ち落とすと得点
- 実装順: まず1Pのみ操縦できる状態 → プレイテスト → 納得したら残りを実装

### 2026-08-12: 技術スタック

- **Phaser 3 + TypeScript + 自作フライトモデル** を採用
- 理由: 描画・音・入力・シーン管理はエンジンに任せ、揚力・失速などの飛行力学だけ自作する。
  汎用物理エンジン（Matter.js 等）は揚力・失速を扱えず、挟むと操作感のチューニングが難しくなるため不採用

### 2026-08-12: 対戦形態

- **1台のキーボードでローカル2人対戦** を採用（P1: 左手側キー、P2: 矢印+右手側キー）
- オンラインは完成後の拡張候補とし、今回のスコープ外
- ゲームパッドと CPU 対戦も当初はスコープ外としていたが、**2026-08-12 に採用**した
  （末尾の「ゲームパッド対応」「コンピュータ操縦」を参照）

### 2026-08-12: サウンド方式

- **全て Web Audio で合成**（SFX はシンセ合成、BGM は1930年代スウィングジャズ風の自作シーケンス）
- 外部素材を使わず自己完結。ただしサンプルを聴いてユーザーが採用判断する（下記「検討中」参照）

### 2026-08-12: 効果音・BGM（採用）

- サンプル第1号（`samples/art-sound-sample.html`）の **SFX 8種と BGM「Dogfight Rag」の方向性を採用**
- Web Audio によるリアルタイム合成方式で本実装する

### 2026-08-12: アートの方式 — コード描画をやめ、提供イラストのスプライト方式に切替

- ユーザーからアートの完成デザイン（機体2色 + 煙あり版 + ステージ背景）が提供された
- 絵柄は**ヴィンテージのペン画 + 水彩**（細密なハッチング、リベット、星型エンジン、木製支柱、張線）。
  コード描画（SVG/Canvas）では再現不可能な密度のため、**コード描画は廃止**
- **提供イラストをそのままゲーム素材として使う**（スプライト方式）
- 第1号〜第3号のコード描画アートはすべて不採用。ただし第3号の**フィルム風ポストエフェクトは有効**で、
  スプライトの上にかける（強度は下げる。詳細は docs/film-effect.md）
- アセットの仕様・必要ファイルは docs/assets.md に定義

## 検討中（採用待ち・実装禁止）

- [ ] ゲームバランス数値の詰め（HP・ダメージ・弾数は提案値のまま。プレイテストで調整する）

## 報告された不具合の調査結果（2026-08-13）

- [ ] **キーとパッドが効かなくなる（Safari。音を鳴らすと再発）** →
      一度は直ったとの報告だったが、**音を鳴らすとまた起きる**という続報。
      調べたところ、前回入れた「描画の輪の覆い」が**一度も掛かっていなかった**うえ、
      輪を生かすだけでは入力は戻らないことが分かった（末尾の項に詳しい）。
      掛け直しと、画面ごとの update の覆い、音の軽量化まで済み。**実機での確認待ち**
- [ ] **Chrome で古いフィルム風のエフェクトが効かない** → 効かなくなる筋道は分かったが、
      どれに当たっているかは実機でないと分からない。分かるように画面に出した（下記）。
      Tab の計器表示に出る「描画」の行を待ち

以下は決定済み。詳細は末尾を参照:

- [x] フィルム風ポストエフェクト → 採用（「弱」と「標準」の中間、縦キズなし）
- [x] スロットルの方式 → 巡航と全開の2段。全開は押している間だけ
- [x] 性能劣化の決め方 → 当たりどころで決まる（機首側=エンジン / 尾翼側=舵）
- [x] 画面端の扱い → ラップアラウンド（本家準拠）
- [x] ボーナス気球 → 金色を入れる。撃つと自機が直る
- [x] 勝利条件 → 20点先取（当初 10点）
- [x] ゲームパッド対応 → 標準配列のパッド専用。ロールは L / R

## 追記（2026-08-12）: アート素材の取り込み

- 原本4点を `assets/art/original/` に保存（無加工）
- 加工物は `tools/cutout.py`（紙地の切り抜き）と `tools/extract_smoke.py`（煙の粒の切り出し）で再生成できる
- 実画面モック `samples/screen-mock.html` で最終確認する

### 2026-08-12: 実画面モック Rev.1 の判定（確定）

- **フィルム処理**: 既定は**「弱」と「標準」の中間**。**縦キズ（フィルムの傷）は不採用**
  ―― 作為的に見えるため。粒状感・ゴミ・露光ゆらぎ・ゲートウィーブ・ビネットは残す
- **ゲートウィーブ**: 現在の量（プリセット「弱」相当）で確定。
  フィルム強度を上げても揺れの量は増やさない（酔いへの配慮。設定でオフにもできる）
- **機体の表示サイズ**: 画面幅の **8%**。煙と発射炎の大きさは機体サイズに追従させる
  （機体だけ縮めると煙が相対的に大きくなり、承認時の見え方が崩れるため）
- **煙**:
  - 通常飛行中も**薄い白煙**を引き、飛行の軌跡が空に残ってしばらくして消える
  - 被弾時の煙は**黒く濃い**。白い軌跡と重なっても被弾が一目でわかる
- **HUD**: クリーム地＋黒枠の意匠を採用
- **武装の弾道**:
  - 7.7mm — 直線・高速（1500 px/秒）・画面全域に届く・連射（毎秒約11発）
  - 20mm — 低速（640 px/秒）・重力で落ちる山なり・射程約 700px・単発
  - どちらも**発射時に機首から炎（マズルフラッシュ）**が出る。20mm は大きく長い炎＋発射煙

### 2026-08-12: 弾道の射程と爆発（確定）

- **7.7mm の軌跡を細く**する（曳光の芯 1.8px・グロー 4.5px）。太いと画面が騒がしくなるため
- **射程を明示的に分ける**。7.7mm は画面幅の約 60%（765px）、20mm は約 37%（480px）。
  20mm は「懐に飛び込んで当てる」武器という位置づけを、数値で保証する
- **爆発は空中撃墜と地面への激突の2種類**を用意する:
  - 空中 — 閃光 → 全方位の火の玉 → 破片が放物線を描く → 黒煙が膨らんで数秒残る
  - 地面 — 火は上半分だけに立ち上がり、土煙が左右に広がる。衝撃波の輪は横長の楕円
- 火・燃え残り・土煙は、**提供イラストの煙の絵を色染めして作る**。別の絵を用意すると絵柄が浮くため。
  染めるとインクの輪郭線まで同じ色になるので、元絵を掛け合わせて線の濃さだけ戻している

### 2026-08-12: ロール操作と気球（追加）

- **ロール操作を追加**する。押すと機軸まわりに180度回り、正立と背面が入れ替わる（0.45秒）
- 見た目は提供いただいた**上面図・下面図を中間コマとして補間**する
  （0°側面 → 90°上面 → 180°側面の上下反転 → 270°下面）
- 操作キーは P1 = R、P2 = /
- **気球**は画面幅の 7.5%。下からランダムに上昇し、7.7mm でも 20mm でも割れる。
  当たり判定は球皮だけ（籠や索には当たらない）

### 2026-08-12: M1 実装時に確定したフライトモデルの数値

すべて `src/config.ts` に集約。プレイテストで触るのはこのファイルだけで済むようにしてある。

- **重力 320・揚力係数 0.015** — 宙返りの大きさが画面に対して見栄えするように決めた。
  実測で 268×272px を 3.1秒で一周する。これより揚力を強くすると、機体より小さい輪で回ってしまう
- **推力 [96, 150]** — 巡航 288、全開 371（のちに全開を 190 = 速度 421 に引き上げ。末尾参照）
- **操縦桿の効き 2.8 / 風見安定 4.0** — この比が、舵を引き切ったときの迎え角を決める。
  速いうちは失速せずに曲がれて、遅くなると失速する
- **舵の効きは速度の平方根で落とす** — 速度に比例させると揚力の項も同じだけ落ちてしまい、
  いくら遅くなっても迎え角が失速角に届かず、失速が起きなくなる
- 実測: アイドルで30度の上昇を続けると 1.4秒・速度145 で失速し、3.7秒で地面に落ちる

### 2026-08-12: 背面飛行の操作（確定）

- **機体基準**。背面では画面上の上下が入れ替わる
- 機体はピッチとロールしかできないため、右へ飛んでいた機が宙返りで左向きになると背面になる。
  正立に戻すにはロールが要る（実機と同じ）。ロール操作が飾りでなく必須の操作になる

### 2026-08-12: M1 プレイテスト第1回の判定

- **スロットルは巡航と全開の2段**にする。全開はボタンを押している間だけで、離すと巡航に戻る。
  3段階の切替式は、操作の手数の割に効果が読みにくかったため。押している間だけにすることで、
  「ここぞという場面で吹かす」という使い方が自然になる
- **エンジン音を小さく**する。鳴りっぱなしの音なので、単発の効果音と同じ音量だと耳につく。
  エンジン専用の音量（`AUDIO.engine`）を設けて 0.45 倍にした
- 飛行の挙動そのものは「全体的にいい感じ」との評価。変更なし

### 2026-08-12: ロールの操作キーを変更

- ロールは **左右キー** に割り当てる（P1 = A / D、P2 = ← / →）。R キーは廃止
- 左右で**回る向き**が変わる。左は下面図を、右は上面図を見せながら回る。
  どちらも 180度 回るので正立と背面の入れ替わりは同じで、見た目の回り方だけが変わる
- 上下（機首）と左右（ロール）が同じ手にまとまり、操作が直感的になる

### 2026-08-12: Git 運用の変更

- **区切りがついた時点で main にマージする**。PR を長く開いたままにせず、
  その都度 main に取り込むことで、main が常に「動く最新版」になっている状態を保つ
- main へ直接 push はしない。作業ブランチ → PR → マージ の流れは維持する
- マージ後は作業ブランチを main から切り直してから次の作業を始める

### 2026-08-12: 低速時の挙動と天井（不具合修正）

「途中でコントロールが効かなくなる」という報告への対処。原因は2つあった。

- **風見安定の効きを速度に比例させていた** ため、機首を上げ続けて速度をほぼ失うと、
  機首が上を向いたまま空中で立ち往生し、操縦不能になったように見えていた。
  効きを速度の平方根にし、下限（`minWeathervane` = 0.3）を設けた。
  落下しはじめれば必ず機首が下を向き、速度が戻って操縦を取り戻せる
- **天井が画面外（y = -40）にあった** ため、高く上がると機体が画面から消えていた。
  機体が見える位置（y = 46）で止め、その手前（y = 150）から下向きの力が強まるようにした。
  硬い壁で跳ね返すより自然で、機体を見失わない
- あわせて、撃墜されている 2.4 秒の間に「撃墜／再出撃まで N」を表示するようにした。
  何も出ないと、この間も操縦不能になったように見えるため

### 2026-08-12: 煙の出し分けとロールの速さ（プレイテスト第2回）

- **通常飛行の白い航跡は廃止**。常時出ていると画面が煩わしいため
- **損傷したときだけ煙を出し、色で当たりどころを示す**:
  - エンジン損傷 → **黒煙**
  - 操作系（舵）損傷 → **白煙**
  - 傷が深いほど間隔が詰まる。両方やられていれば両方の色が出る
- **ロールは 0.45秒 → 0.85秒**。一瞬すぎて手応えがなかったため。
  ロール中は画面内に働く揚力が減るので、遅くしたぶん高度も落ちる（機動としての代償が増える）

### 2026-08-12: キーが押しっぱなしになる不具合

- キーを押したまま別のタブやウィンドウへ移ると、キーを離したイベントが届かず、
  戻ってきたときに押しっぱなしの状態が残っていた
- 離脱と復帰の両方で押下状態を消すようにした。ブラウザのイベント（blur / focus /
  visibilitychange）は環境によって飛んでこないことがあるため、
  Phaser 側の通知（BLUR / FOCUS / PAUSE / RESUME / HIDDEN / VISIBLE）も併せて拾う

### 2026-08-12: 押しっぱなしの自動解除（Safari 対策）

上の修正のあとも Safari だけで押しっぱなしが残るという報告があった。Chrome では起きない。
特定のブラウザだけを狙って直すのではなく、**どのブラウザでも自力で復帰する仕組み**にした。

- **キーリピートを生存確認に使う**（`src/input/StuckKeyGuard.ts`）。
  キーを押し続けている間、ブラウザは keydown を繰り返し送ってくる。
  「押されているはずなのに、しばらく keydown が来ていないキー」は、
  実際にはもう離されているとみなして解除する
- **見送り時間は 1.6 秒**。OS のキーリピートが始まるまでの待ちは最長でも 1 秒ほどなので、
  それを十分に超える値にしないと、本当に押し続けているキーを誤って解除してしまう
- **繰り返しの keydown を一度も観測していないうちは働かない**。
  リピートを送らない環境で誤作動させないための安全弁
- デバッグ表示（Tab）に、いま押されているキーと解除した回数を出すようにした。
  効いているかどうかをユーザーが自分で確認できる
- 注記: この環境に WebKit を入れられなかった（Playwright のダウンロードが失敗する）ため、
  **Safari 実機での確認はできていない**。keyup が失われる状況を人工的に作って、
  1.7 秒で解除されることと、押し続けている間は解除されないことは確認済み

### 2026-08-12: 長時間プレイでフィルムの質感が消える問題

「しばらく実行しているとフィルタが取れていく」という報告への対処。
この環境（ソフトウェア描画）では再現しなかったが、原因として考えられるものを両方塞いだ。

- **時間の精度落ち**。粒とゆらぎは時間を種にしているので、時間が増え続けると
  浮動小数の精度が足りなくなり、粒が動かなくなって「膜が剥がれた」ように見える。
  時間を 16 秒（= 1/24 秒 x 384 コマ）で折り返し、シェーダの精度も
  使える環境では高精度（highp）を指定するようにした
- **時間の送り元を自前に**。ゲーム側から毎フレーム送る形だと、
  何かの拍子にパイプラインが作り直されたときに時間が進まなくなる。
  パイプライン自身が描画のたびに時刻を読むようにした
- **見張りを置いた**。カメラからエフェクトが外れていたら付け直す。
  強制的に外して 1 秒以内に復帰し、強度も保たれることを確認済み

### 2026-08-12: ゲームパッド対応（採用）

当初は「完成後の拡張候補・今回のスコープ外」としていたが、操作を詰める過程で採用した。

- **標準配列（mapping === 'standard'）のパッド専用**。PlayStation / Switch Pro / Xbox など。
  標準でないパッドは並びが機種固有で、番号をそのまま読むと別のボタンが操作に化けるため、
  つながっていても使わない。対応機種を絞ることで、番号の推測や機種ごとの分岐が要らなくなる
- **ロールは L / R ボタン**（ボタン 4 / 5）。スティック左右に置く案もあったが、
  機首を引きながら旋回するとスティックが斜めになり、ロールが暴発するため肩ボタンにした
- **機首はアナログ**。スティックを倒した量がそのまま舵の効きになる。
  飛行モデルは元から連続値を受け取る作りなので、キーボードの3値（引く／中立／押す）より
  細かく操れる。十字キーは倒し切りとして扱う
- 全開 = R2 / ZR（アナログのトリガーなので、浅く握っただけで全開にならないようしきい値を設けた）、
  7.7mm = ×／B、20mm = ○／A
- キーボードと同時に使える。両方触っている場合は深く入れているほうを採る
- **毎フレーム状態を読みに行く方式**にした。ブラウザのイベントに頼らないので、
  キーボードで悩まされた「押しっぱなしが残る」問題が原理的に起きない。
  パッドを抜き挿ししても、ボタンの立ち上がりの記録が残って暴発することはない

### 2026-08-12: 起動直後に甲高い音が鳴る不具合

「スタートした時に変な高い音が鳴っていて、エンジンを吹かすと通常のエンジン音になる」
という報告への対処。原因は2つが重なっていた。

- **発振器の初期値が 440Hz（ラの音）だった**。Web Audio の発振器は何も指定しないと
  440Hz で鳴る。エンジン音は基音の発振器2つに加えて、爆発の刻みを作る発振器で
  音量を揺らしているが、その刻みまで 440Hz で回るため、機関の音ではなく甲高い音になっていた
- **段階を決める呼び出しが、音を鳴らせるようになる前に空振りしていた**。
  ブラウザは操作があるまで音を鳴らせないので、エンジン音の実体ができるのは
  ゲームが動きはじめたあと。ところがゲーム側は「段階が変わったときだけ設定する」
  作りだったため、実体ができる前の1回で「設定済み」と記録してしまい、
  実体には一度も届かなかった。スロットルを踏んで段階が変わって初めて設定が届く
  ―― これが「吹かすと通常の音になる」の正体
- 直し方: 発振器を作った時点で目的の段階に合わせる。あわせて、
  音を鳴らせるようになる前に要求された段階を覚えておき、鳴らしはじめるときに適用する

### 2026-08-12: M2 に入るにあたっての決定（採用）

「検討中」に残っていた項目のうち、M2 の実装に必要なものを確定した。

- **勝利条件は 10点先取**。撃墜3点・気球1点・相手の自滅1点（提案どおり）。
  撃墜3〜4回ぶんで、1試合5分程度になる
- **性能劣化は当たりどころで決まる**（ランダムから変更）。
  機首側に当たればエンジン、尾翼側に当たれば舵。狙って壊し分けられるようになり、
  相手の煙の色（黒＝エンジン／白＝舵）で結果が読める。
  判定は、被弾点を機軸に射影して前後どちらかを見るだけ
- **金色のボーナス気球を入れる。撃つと自機が直る**（得点も通常の気球と同じ1点）。
  HP と性能低下の両方が戻る。負けている側の逆転手段になり、
  試合終盤まで気球を撃つ動機が続く。出る割合は 22%（`BALLOON.goldChance`）
- 金色は**色を染めたうえで後光を添える**。染めるだけでは夕焼けの空に埋もれて
  普通の気球と見分けがつかなかったため。後光は煙の絵を黄色く染めたものを流用している
  （別の絵を用意すると絵柄が浮くため）
- **画面端のラップアラウンド**（本家準拠）は M1 で実装済みのまま確定とする

### 2026-08-12: M2 の実装内容

- **2機が同じ空を飛ぶ**。1P は左から右向き、2P は右から左向きに出撃する
- 操作は 1P = W/S・A/D・E・F・G、2P = ↑/↓・←/→・Shift・, ・. 。
  上下が機首、左右がロールで両者そろえてある
- **ゲームパッドは2台まで**。標準配列のパッドをつないだ順に 1P・2P へ割り当てる
- **エンジン音は機体ごとに鳴らす**。相手の被弾による息継ぎが自分の音に混ざると
  何が起きたか分からなくなるため。2つ重なるぶん、まとめて音量を絞っている
- **計器は左右対称**に置く。名札と得点を画面の端側に寄せ、視線が自陣側にまとまるようにした
- 勝敗が決まると操作を受け付けなくなり、その場で静止する。Enter でやり直し
- M1 のテスト用キー（Z/X/C の被弾・修理）は廃止。相手がいるので要らなくなった

### 2026-08-12: コンピュータ操縦（採用）

当初は「完成後の拡張候補・今回のスコープ外」としていたが、
1人でも動かして確かめたいという要望から採用した。

- **AI は人と同じものしか出さない** ―― 機首の引き具合・ロール・スロットル・引き金の5つ。
  機体の中身を直接いじらないので、人に出せない動きにはならないし、
  失速も墜落も人と同じように起きる。飛行モデルを迂回する近道は作らない
- **考え方は「機首をどこへ向けたいか」を1つの角度に決めるだけ**。優先順位は
  地面が近い → 失速している → 狙う相手（敵機か気球）の未来位置、の順。
  地面と失速は何を措いても最優先にしないと、AI は真っ先に墜ちる
- **ロールを実機と同じ理由で使う**。機首を回すのに「押す」より「引く」ほうが素直なので、
  押し続けることになる場面では背面に返して引きに変える。
  ただし地面や失速から逃げている最中は回っている余裕がないので我慢する
- **腕前は3段階（弱・普通・強）**。違うのは狙いの許容範囲・迷ってから動くまでの間・
  未来位置の読みの正確さ・狙いの揺らぎ・撃ちはじめる距離・20mm を使うか。
  「弱」は 20mm を使わず、狙いも大きく揺れる
- **切り替えはキー1つで巡回**（切 → 弱 → 普通 → 強 → 切）。1P = V、2P = C。
  2P を任せれば1人で遊べ、両方に任せればデモとして眺められる
- 撃つと決めてから実際に引くまでに間を置く。反射で撃たれると理不尽に感じるため

### 2026-08-12: コンピュータ操縦の詰め（実測して直した3点）

最初の実装では、CPU 同士が 15秒で 11対1 になり、地面にもよく墜ちていた。
実測して原因を1つずつ潰した。

- **引き起こしは「今の高さ」ではなく「1.1秒先の高さ」で判断する**。
  今の高さだけで見ると、突っ込んでいる最中は手遅れになる。
  宙返りの半径が 135px ほどあるうえ、迷ってから動くまでの間もあるため。
  これで地面への自滅が 180秒あたり 28〜40回 → 0回になった
- **狙う高さに底を設ける**（地面から 150px）。地面すれすれの相手を追って
  一緒に突っ込まないようにする。撃つのを諦める代わりに墜ちない
- **失速させないために迎え角そのものを見る**。舵を引くと迎え角が増えるので、
  失速角の 0.8 倍を超えたら緩める。逆向き（迎え角を減らす向き）の舵は妨げない。
  速度で加減する案も試したが効かなかった ―― 失速を決めているのは迎え角なので、
  そちらを直接見るほうが正しい。失速していた割合が 11〜33% → 1〜12% に下がった

**実測値（各180秒）**

| | 弱 | 普通 | 強 |
|---|---|---|---|
| 何もしない相手への得点 | 46 | 85 | 95 |
| CPU 同士の決着数 | 3試合 | 3試合 | 7試合 |
| CPU 同士の得点 | 30対16 | 37対32 | 66対69 |
| 地面への自滅 | 0回 | 0回 | 0回 |
| 失速していた割合 | 8〜12% | 7〜8% | 9% |

同じ腕前どうしなら互角に、腕前が上がるほど強く速くなる。
なお「何もしない相手」は動かないので実際の人より与しやすい。

### 2026-08-12: 再出撃時の無敵・被弾時の震え・BGM の自動再生（採用）

- **再出撃直後は 2秒の無敵**。出てきたところを撃たれて何もできないのを防ぐ。
  弾は当たらずすり抜ける。CPU 同士だと、この隙がないと一方的な展開になりやすかった
- 無敵中は**機体が点滅して透ける**（毎秒6往復、濃さ 0.75 ⇄ 0.28）。
  見ただけで「今は当たらない」と分かるようにするため
- **被弾して傷むと機体が震える**。傷が深いほど大きく震える（最大 3.4px）。
  縦と横で周期をずらしてあるので、円を描かずブルブルと細かく揺れる。
  揺らすのは**見た目だけで、当たり判定は動かさない** ―― 傷ついた側が
  当たりにくくなってしまっては本末転倒なので
- **BGM は最初から鳴らす**。ブラウザの決まりで音を出せるようになった時点
  （最初のキー入力かクリック）で始める。B キーで止められるのは今までどおり
- CPU は無敵中の相手を撃たない。当たらないので弾の無駄になるため。
  ただし狙いは付け続ける（無敵が切れた瞬間に射線へ乗っているように）

### 2026-08-12: 全開の推力を引き上げ（150 → 190）

「スロットルを押したときのパワーをもう少し、加速したときのスピードをもう少し欲しい」
という要望への対応。**全開だけ**を上げ、巡航は変えない。

- 巡航 288 のまま、**全開 371 → 421**（+13%）
- 巡航との差が **83 → 133** に広がった。吹かす価値が上がり、
  「ここぞという場面で吹かす」という設計が効くようになる
- 加速も速くなった。速度差の9割に届くまで **3.5秒 → 3.0秒**
- 宙返りは 210×297 → 206×321。速いぶん輪はやや縦に伸びるが、画面には収まる
- 急上昇からの立て直しはむしろ楽になった（最低速度 92 → 143）

`tools/flight-probe.mjs` に**加速の測定**を足した。最高速だけ上げても、
そこへ届くのに時間がかかると速くなった気がしないため、
「巡航から全開にして、速度差の半分／9割に届くまでの秒数」を出すようにした。

### 2026-08-12: 気球を増やし、勝利点を 20点先取に

プレイテストの評価は「全体的にとてもいい感じ」。そのうえでの調整2点。

- **勝利点を 10点 → 20点先取**（`SCORE.winning`）。1試合が短く感じられたため
- **気球の出現間隔を 8〜20秒 → 4〜11秒**、**同時に浮かぶ上限を 3 → 4**。
  間隔だけ縮めても上限で頭打ちになる ―― 気球は上りきるまで 18〜28秒 空にいるので、
  実際の数を決めているのは上限のほう。両方を上げないと増えない

**実測（CPU「普通」同士・300秒）**

| | 前 | 後 |
|---|---|---|
| 平均で空にいる気球 | 1.18 | **2.01** |
| 気球が1つもない時間 | 15% | **3%** |
| 割られた気球の数 | 18 | **33** |

「撃つものがない時間」がほぼ無くなった。20点先取にしたぶん試合が長くなるので、
気球を増やしておかないと中だるみしやすい ―― 2つの変更は互いを支えている。

**試合の長さ（CPU 同士・実測）**: 弱 125〜162秒 / 普通 63〜125秒 / 強 44〜65秒。
人が操縦すればこれより長くなる。

### 2026-08-12: タイトル画面と開始の合図（採用・M3）

「1人デモはどう遊ぶのか」「最初に 1P か 2P を選べるようにしたい」
「スタート時に ready? やカウントダウンを入れたい」への対応。

- **タイトル画面**（`src/scenes/TitleScene.ts`）を追加し、起動時はここから始まる。
  遊び方は **1人で遊ぶ / 2人で対戦 / デモを見る** の3つ。
  コンピュータの腕前（弱・普通・強）もここで選ぶ。
  「2人で対戦」のときは腕前を使わないので、薄く表示して効かないことを示す
- 操作は **上下で遊び方、左右で腕前、Enter で決定**。
  パッドでも選べる（スティック上下で遊び方、L / R で腕前、○／A で決定）
- **開始の合図**を入れた。**READY? → 3 → 2 → 1 → GO!** の順に約3秒。
  数字ごとに低い音、GO! で高い音が鳴る
- 合図の間は**機体を出撃位置で止めて待たせる**。飛ばしたまま待たせると、
  始まる前に落ちたり流されたりしてしまう
- やり直し（決着後の Enter）にも合図を入れる。いきなり始まると身構える間がない
- 対戦中の **Esc でタイトルへ戻れる**
- **音は画面をまたいで1つだけ持つ**ようにした（`export const sfx`）。
  画面ごとに作ると AudioContext が増え、BGM が二重に鳴ったり、
  タイトルで鳴らしはじめた曲が対戦に移った瞬間に止まったりする
- 遊び方と腕前は、対戦から戻ってきても**前回選んだものが残る**。すぐ再開できるように

### 2026-08-12: プラクティス（採用・M3）

操縦の練習用のモード。ユーザー提案をそのまま実装した。

- タイトルの遊び方に **プラクティス** を追加（1人 / 2人 / プラクティス / デモ の4つ）
- **全10ステージ**。番号の付いた輪が空に浮かび、**番号順に**くぐる。
  順番どおりでない輪は消えない（判定するのは常に「次の1つ」だけなので、実装も単純）
- 背景は対戦と同じものを流用。敵も武装も出さない ―― 練習が目的なので
- **輪の並びは毎回同じ**。ステージ番号を種にした擬似乱数で組み立てる。
  毎回変えるとタイムを比べられず、ハイスコアの意味がなくなるため
- 難しさは仕様どおり**「輪の数」と「順番の厳しさ」**の2つで上げる。輪の大きさは変えない
  - 数は 3 個から 1 つずつ増えて 12 個
  - 順番は「次にどれを選ぶか」で決める。やさしいステージは近い輪から、
    難しいステージは遠い輪から辿らせる。実測で、次の輪までの平均が 258px → 456px に伸びる
- **置き場所は升目から選んで升の中でずらす**。適当に撒いて重なりを弾く方式だと、
  輪が増えたときに置ききれず数が足りなくなった（10〜12個のはずが 8〜10個になっていた）。
  列ごとに上下へずらして、升目に並んで見えないようにしてある
- くぐった判定は**機体の中心が輪の内側に入ったか**。輪の面を横切ったかまで見ると、
  真横から入らないと通れず窮屈になる
- **各ステージのタイムを測り、10ステージの合計がいちばん短いものをハイスコア**として
  localStorage に残す（`biplanes.practice.best`）。使えない環境でも落ちないようにしてある
- 落ちてもすぐ出撃位置に戻すが**時計は止めない**。落ちること自体が損になる
- **R** で今のステージをやり直し、**Esc** でタイトルへ
- 開始の合図は対戦と同じものを使う。共通部品として `src/ui/Countdown.ts` に切り出した

### 2026-08-12: プラクティスの改定（横から潜る・手組みのコース）

初版は「輪の中に入れば通過」「配置は擬似乱数」だったが、ユーザーの指摘を受けて作り直した。

- **輪にくぐる向きを持たせた**。その向きに輪の面を横切ったときだけ通過になる。
  上から乗っても、逆向きに抜けても通らない。この制約があると
  「次の輪へどの向きで入るか」を逆算して飛ぶ必要が生まれ、練習として成立する
- 判定は**前のフレームと今のフレームを結んだ線分が面を越えたか**で見る。
  位置だけで見ると輪の中へ入り込んだ時点で通ってしまい、横から潜る意味がなくなる。
  線分で見れば速く飛んでも飛び越さない
- 見た目は**くぐる向きに潰した楕円**（潰し 0.30）。潰れている方向が入る向きを表す。
  読み取りやすいよう矢印も添える
- **配置と順番は全ステージ手で組む**。乱数だと「この配置ならこの機動」という狙いが作れない。
  上昇と下降 → 折り返し → 宙返り → スプリットS → インメルマンターン →
  八の字 → 一周 → 連続旋回 → 総仕上げ、の順に必要な機動が増える
- 輪の位置は**機体が実際に描ける輪の大きさに合わせる**。宙返りは 206×321px なので、
  折り返しの上下差は 320px 前後、横幅は 200px 前後。これを外すと辿れないコースになる
- 同じ場所に輪を2つ置かない。番号が重なってどちらを狙うのか読めなくなる

**確認のしかた**: 実物の飛行モデルで輪を追う自動操縦を書き、全10ステージを
通しで回れるかを測った。回れないコースは「置き方が機体の旋回半径に合っていない」ことを
示すので、そこを直した。最終的に全10ステージ、墜落0回でクリアできている。

### 2026-08-13: オープニングとステージ選択、金の気球の絵

いただいたオリジナル画像を取り込んだ。

- **オープニングは完成図（`opening-title.png`）をそのまま一枚で出す。**
  はじめは背景・ロゴ・赤機・青機を切り抜いて組み直し、完成図に重ねて位置を
  合わせようとしたが、絵がそのまま使えるならそのほうが早く、確実に狙いどおりになる
- 絵に描かれている **"PRESS START" は消しておき**、代わりに
  **PRESS A BUTTON OR ENTER KEY** を明滅させる。押せば進むことが動きで分かるため。
  消し方は `tools/cutout.py` の `opening_title()`（文字が乗っているのは一面の海なので、
  同じ行の海を横からずらして持ってきて埋める。海は横縞なので継ぎ目が出ない）
- **次の画面はステージ選択**。背景はロゴと機体を除いた空だけの絵（`opening-background.png`）。
  遊び方と腕前の選び方はこれまでどおり。Esc でオープニングへ戻る
- **金の気球は専用の絵に差し替え**（`gold-baloon.png`）。それまでは通常の気球を
  色染めして後光を添えていたが、絵があるなら要らない

### 2026-08-13: 上下の操作を反転（引くと機首が上がる）

キーボードもパッドも、**下（引く）で上昇・上（押す）で下降**にした。操縦桿と同じ向き。

- 反転させるのは**人の操作だけ**。AI が出す `pitch` は機体基準のままなので触らない
  （`flight.ts` で反転させると AI まで裏返る）
- パッドの `PadInput.pitch` は「スティックを上に倒すと正」の意味のまま残し、
  機体に渡すところで符号を反転する。ステージ選択のカーソル移動が
  スティック上下のままで済むため
- 背面飛行では画面上の上下が入れ替わるのは今までどおり（機体基準）

### 2026-08-13: ステージ選択の BGM と効果音（採用）

`samples/menu-audio-sample.html` の聴き比べから、ユーザーが選んだもの。

- **BGM は「ニュース映画の行進曲」**（2拍子・116拍。チューバのオムパ、小太鼓のロール、
  金管のファンファーレ）。**レコードのノイズは入れない**（指定）。
  対戦の曲とは拍子で差が付くので、画面が変わったことが音でも分かる
- **カーソル移動は「小さなチャイム」**（5度上に跳ねる2音）。
  腕前の左右は同じ音を、弱いほうへは低く・強いほうへは高くしたもの
- **決定は「小さなファンファーレ」**（金管で ド・ミ・ソ・ド）。戻るは下がる2音
- 鳴らす場所: オープニングは無音（絵だけの画面）。ステージ選択で曲と効果音。
  対戦とプラクティスに入ると自動で対戦の曲へ入れ替わる。B キーの入／切はそのまま

**作りの要点**

- `src/audio.ts` の曲を `Track`（手を並べる土台）と、`BattleBgm` / `MenuBgm` に分けた。
  `sfx.playBgm('menu' | 'battle')` で切り替え、同じ曲なら何もしない ――
  画面を作り直すたびに頭から鳴り直すのを防ぐため
- B キーで切った状態は覚えておき、画面が変わっても切ったままにする
- **曲ごとに音量をそろえる `level` を持たせた**。マーチは同時に鳴る音が多く、
  そのままだと対戦の曲より 2.5 倍ほど大きかった（実効値で実測してならした）

### 2026-08-13: 「キーを一度押すと以後効かなくなる」の原因と対処

**原因は Phaser のキーの持ち方にあった。** `Key.onDown` は `isDown` が false のときしか
「押された」合図を出さない（`node_modules/phaser/src/input/keyboard/keys/Key.js`）。
KeyboardPlugin の `keydown` も同じで、押しっぱなしのキーには出さない。

つまり **離した合図（keyup）をひとつ取りこぼすと、そのキーは二度と反応しなくなる**。
メニューの上下も、ロールも、機関砲も、押した瞬間の合図で動かしているので全部死ぬ。
「一度押したらそれきり効かない」という症状はこれで起きる。

**対処は2つ。**

1. **押し直しを取りこぼしの証拠として使う。** 押しっぱなし扱いのキーに、
   繰り返しでない keydown が届いたら、離し損ねている。その場で解除してから
   Phaser に処理させる（こちらは捕捉フェーズで先に受け取る）。
   押しっぱなしの繰り返しと読み違えないよう、前の keydown から 0.9 秒以上
   空いているときだけ解除する
2. **メニューにも見張りを付けた。** それまで `StuckKeyGuard` は対戦とプラクティスに
   しか付いておらず、ステージ選択は素のままだった

**確認のしかた**: keyup を握りつぶすブラウザを作って再現した。
直す前は1回目のあと二度と選べなくなり、直したあとは2回目以降も選べる。

### 2026-08-13: 「Chrome でフィルム効果が効かない」の調べ

**効かなくなる筋道は2つ。** どちらも黙って絵が変わらないだけなので、区別が付かなかった。

1. **WebGL が使えず Canvas での描画に落ちている。** Phaser は `AUTO` で自動的に切り替わり、
   ポストエフェクトの仕組みごと無くなる。実測でも Canvas ではカメラに何も付かなかった
2. WebGL はあるが、何かの理由でパイプラインが載らない

**どちらなのか画面で分かるようにした。** 起動時に Canvas なら但し書きを出し、
Tab の計器表示にも描画方式（WebGL / Canvas）と GPU 名を出す。

WebGL があるときは、この環境の Chrome でもフィルム効果は正しく掛かっている
（強さ「切」と「強」で描画が変わることを実測）。実機で Tab を押して確かめてほしい。

### 2026-08-13: 例外で画面が固まらないようにする

Phaser は毎フレームの呼び出しを済ませてから次のフレームを予約する
（`RequestAnimationFrame.step`）。**途中で例外が出ると次の予約に届かず、そこで止まる。**
絵は最後のコマのまま残るので、見た目には「キーが効かなくなった」ように映る。

`src/diagnostics.ts` で毎フレームの呼び出しを包み、例外が出ても輪を回し続けるようにした。
出た例外はキャンバスの外の DOM に出す（輪が止まっていても読めるように）。
音の用意も、入力の処理の中から呼ばれるので、失敗しても外へ投げないようにした。

### 2026-08-13: デモを廃してフリープレイに

**コンピュータどうしのデモは外した。** 代わりに**フリープレイ** ―― 相手を出さず、
一人で飛ぶだけのモードを入れる。

- 機体は 1P だけ。**勝ち負けは付かない**（点数の上限に達しても終わらない）
- 気球は出る。撃った数を計器に「気球 n」と出す（`n / 20` の表示はしない）
- 落ちればこれまでどおり再出撃する。時間の制限もない
- 対戦の画面（`PlayScene`）をそのまま使い、`init` に `free: true` を渡すだけ。
  中は元から players を舐めて回る書き方なので、機体が1機でもそのまま動く
- **V キーで自機をコンピュータに任せられるのは残す**。眺めたいときはこれで足りる

### 2026-08-13: 「一度押すと以後効かなくなる」の続報 ―― パッドでも起きる

**キーボードだけの問題ではなかった。** パッドでも同じ症状が出るという報告を受けた。
パッドは毎フレーム今の状態を読みに行く作りで、キーの押しっぱなしとは無関係なので、
前回の見立て（Phaser のキーが押しっぱなしのまま固まる）では説明が付かない。

**両方に共通するのは「最初の入力で音を用意しに行く」こと。** キーなら最初の keydown、
パッドなら最初に認識したときに `wakeAudio()` が走る。ここで例外が出ると、
Phaser は毎フレームの処理を済ませてから次のフレームを予約する作りなので画面がその場で止まり、
**原因が音でも症状は「キーもパッドも効かない」になる**。

打った手は4つ。どれも「音が原因なら、もう止まらない」ようにするもの。

1. **音の入口をすべて覆った**（`neverThrows`）。外から呼ばれる口を Proxy で包み、
   一度でも失敗したら以後は音なしで進む。個々のメソッドに try を書く方式だと、
   あとで足したものを包み忘れるため
2. **Phaser 自身の音を切った**（`audio: { noAudio: true }`）。音はすべて自前で合成しており
   Phaser の音は使っていないのに、起動時に AudioContext がもう一つ作られていた。
   Safari は同時に持てる数が少ないので、無駄には持たない
3. **パッドの読み取りも落ちないようにした**。ここも毎フレーム、画面の処理の中から呼ばれる
4. **開始の合図を実時刻で測るようにした**（下記）

### 2026-08-13: 開始の合図を実時刻で測る

合図の間は操作を受け付けない。その長さを「1コマぶんの刻みの足し算」で測っていたが、
**刻みは 0.05 秒で頭打ちにしてある**（物理が飛ばないように）ので、遅い機械では
実時間が延びる ―― 実測で 14 コマ/秒だと、3 秒の合図が **11 秒**かかっていた。
その間ずっと「押しても効かない」ので、症状の一部はこれだった可能性がある。

`performance.now()` で測るようにして、コマ数によらず 3.05 秒で終わるようにした。
合図の間は機体を出撃位置に止めているので、実時刻で測っても物理には影響しない。

### 2026-08-13: 止まったことを、止まった側から知らせる

「操作を受け付けない」という症状は、**絵が止まっているのか、絵は動いていて入力だけ通らないのか**で
原因がまるで違う。`setInterval` は描画の輪とは別に動くので、そこからコマ数の進みを見張り、
1.5 秒進んでいなければ画面に出す。輪が死んでいても読める。

### 2026-08-13: 計器まわりを作り直した（参考図どおりの真鍮の計器盤）

こちらから出した3案（`samples/hud-design-sample.html`）は**すべて不採用**。
代わりに**ユーザーから参考図が提供された** ―― 濃紺の板に真鍮の縁と鋲、
クリーム色の銘板、走行距離計のような数字の窓、区切りのある体力の帯、右端に丸い計器。
これに合わせて作り直した（`src/ui/Panel.ts`）。

| 参考図 | ゲームでの役割 |
|---|---|
| SCORE（数字の窓） | 得点。3桁を頭 0 で埋める。上限は窓の隅に小さく `/ 20`（フリープレイでは出さない） |
| HEALTH（区切りのある帯） | 体力。8目盛り。左から赤 → 橙 → 黄で、減ると右から消え、最後に赤だけが残る |
| 盾の飾り | 1P / 2P の色分け。持ち主の色を入れる |
| WATER TEMP（丸い計器） | **スロットル**にした。緑・黄・赤の帯と針。針は目標へ滑らかに寄せる |
| （参考図に無し） | 20mm の残弾。掘り込んだ受け皿に真鍮の弾を5発。撃つと沈んで暗くなる |

- **1P・2P で中身の並びは同じ**にする。鏡にすると銘板の文字まで裏返って読みにくい
- **枠と銘板は一度だけ描く**。毎フレーム描き直すのは数字・体力・針・残弾だけ
- 塗りの剥げは種を決めた乱数で散らし、毎回同じ場所に出るようにしてある
- **絵そのものは使っていない**（提供されたのは画面内の画像で、ファイルは未取得）。
  原本を `assets/art/original/` に置いてもらえれば、枠は描画ではなくその絵に差し替えられる

### 2026-08-13: 水温計と、全開の強化（試行 ―― 合わなければ戻す）

丸い計器を参考図どおり**水温計**として使い、全開に代償を付ける。
あわせて全開の推力を上げ、「逃げる・登る」ための手として使えるようにした。

- **全開の推力 190 → 250。** 実測で全開の速度 421 → 485、巡航との差 133 → 197。
  9割の速度まで 2.6 秒
- **水温は全開で上がり、巡航でゆっくり下がる。** 下限は 0.30（計器の針もそこまでしか戻らない）
- **振り切れたまま吹かし続けると、1.1 秒ごとにエンジンが傷む。** 損傷は 7.7mm 一発ぶんと同じ。
  弾を受けたのと同じ扱いなので、黒煙が出て震え、機体の性能も落ちる
- **赤帯に入ると針が赤くなる。** 数字を読まなくても危ないと分かる

**実測（時間はゲーム内の秒）**

| | 秒 |
|---|---|
| 冷えきり → 赤帯 | 4.3 |
| 冷えきり → 振り切れ | 6.7 |
| 冷えきり → 最初の焼き付き | 7.7 |
| 振り切れ → 赤帯を出る | 4.5 |
| 振り切れ → 冷えきり | 12.7 |

上げ下げの比は約 2:1。**全開でいられるのは、おおむね時間の3分の1まで**。
吹かしっぱなしだと 15 秒ほどで自滅する（HP が 7 回ぶんで尽きる）。

**副作用**: 推力を上げたので、**全開のあいだは舵を引き切っても失速しなくなった**
（巡航では今までどおり失速する）。全開は「短時間だけ強い」手になった。

**コンピュータにも計器を見せた**（`src/ai/Pilot.ts`）。赤帯に入ったら緩め、
0.20 ぶん冷えるまで我慢する。人に見える情報しか使わない方針は変えていない。
実測で 70 秒回して、振り切れ 0 回・水温の最大 0.75（＝赤帯の入口で止まっている）。

### 2026-08-13: 赤帯ではエンジン音が変わる

計器を見ていなくても、耳だけで「今まずい」と分かるようにする。
水温が赤帯に入っている間だけ、`EngineVoice` の鳴りを3つ変える（`src/audio.ts`）。

| | 平常 | 赤帯 |
|---|---|---|
| 基音・刻み | 88Hz / 34Hz（全開） | **×1.07**（94Hz / 36Hz）。回転が上ずる |
| 低域通過フィルタ | 300 + 段階×330 | **+620Hz**。倍音が通ってざらつく |
| うなり | なし | 6.4Hz で **±42 セント**。回りが不揃いになる |
| ノッキング | なし | **金属を叩く音を 130ms ごと**（4回に1回は抜かす → 約 5.8 回/秒） |

**赤帯を出るまで鳴り続ける。** スロットルを離しても水温が下がりきるまでは
音が荒れたままで、これが「まだ冷えていない」の合図になる。

**音の大きさは変えない。** 危ないことは音色で伝えるもので、音量で伝えるものではない
（うるさくなるだけで、相手の音や着弾音が聞こえなくなる）。実測:

| | 平常 | 赤帯 |
|---|---|---|
| 実効値 | 0.0975 | 0.0990（**+2%**） |
| 尖頭値 | 0.333 | 0.399 |
| 1〜4kHz の量 | 0.0513 | 0.0748（**1.46 倍**） |

ノッキングだけを切り出すと **尖頭 0.196 / 実効 0.0105**。エンジン単体（0.332 / 0.0996）に対し、
**尖頭では -4.6dB まで迫るのに、平均では -19.5dB しか足さない** ―― 短い打音なので
はっきり聞こえるが、音の厚みは増えない。この形にしたかった。

初めは打音を 1.5kHz・Q=7 で作っていたが、方形波の倍音がその帯にほとんど無く、
帯域の量が 4% しか動かなかった（＝ほぼ聞こえない）。**1.05〜1.5kHz・Q=3.2** に広げ、
振幅も上げて上の数字になった。

**測り方の注意（1）**: エンジンの音量 `g.gain` は刻みの発振器に変調されているので、
`setValueAtTime(0)` を書いても消えない（変調ぶんが残る）。切り分けるときは
`lp.disconnect()` で経路そのものを切ること。これを間違えて、
打音だけのはずの実効値が 2 回続けて同じ値になり、しばらく気づかなかった。

### 2026-08-13: 全開を 220 に戻し、水温を早く上げる

試行した水温計（上の項）を実際に遊んだうえでの調整。

- **全開の推力 250 → 220。** 速すぎたので少し戻した。
  実測で全開の速度 485 → **454**（巡航 288 との差 166）
- **水温の上がり方 0.105 → 0.14（毎秒）。** 吹かせる時間を短くする

| | 前 | 今 |
|---|---|---|
| 冷えきり → 赤帯 | 4.3 秒 | **3.2 秒** |
| 冷えきり → 振り切れ | 6.7 秒 | **5.0 秒** |
| 冷えきり → 最初の焼き付き | 7.7 秒 | **6.1 秒** |
| 吹かしっぱなしで自滅 | 約 15 秒 | **約 13 秒** |

冷える側（`tempFall` 0.055）は動かしていない ―― 赤帯を出るまで 4.5 秒、冷えきるまで 12.7 秒。
上げ下げの比は 2:1 から **2.5:1** になり、全開でいられるのは時間の3割弱まで。

### 2026-08-13: 「音を鳴らすと操作が効かなくなる」の本当の原因

**包みが掛かっていなかった。** 2026-08-13 に入れた「描画の輪を落とさない覆い」
（`guardLoop`）は、**一度も効いていなかった**。

Phaser は起動のとき `ready` を出してから輪を回しはじめる（`Game.boot`: `emit(READY)` の
**次の行**が `start()`）。こちらは `ready` で `raf.callback` を包んでいたが、
包んだ相手はまだ `NOOP` で、直後に `raf.start()` が本物を代入して包みごと消していた。
画面から目を離して戻ったとき（`TimeStep.wake`）にも同じことが起きる。

→ **毎フレーム掛け直す**ようにした（掛け済みなら何もしない）。

**それでも足りなかった。** 輪を生かすだけでは操作は戻らない。Phaser の1フレームは

```
画面の update → 描画前の処理（PRE_RENDER）→ 描画
```

の順で走り、**キーとパッドの読み取りは「描画前の処理」でまとめて行われる**
（`InputManager.preRender`）。つまり画面の update で例外が出ると、そこから先へ進めず、
**絵が止まるだけでなくキーもパッドも一切読まれなくなる**。
輪の外側で受け止めても、毎フレーム同じところで落ちれば入力は死んだままになる。

→ **画面ごとの update も包む**ようにした（`sys.sceneUpdate` を差し替え）。
実測: update が毎フレーム例外を投げる状態にしても、2 秒で 23 コマ進み、
描画前の処理も 23 回走る（包む前は 0 コマ・0 回）。

**音そのものも軽くした。**

- **雑音の素を毎回作っていた。** 音を鳴らすたびに `createBuffer` して `Math.random()` で
  埋めていた。機銃は 0.09 秒ごとに鳴るので、2機ぶんで**毎秒 42 万回の乱数と 1.7MB の確保**。
  BGM のシンバルや刻みも同じだった。→ **3 秒ぶんを一度だけ作って使い回す**。
  単調にならないよう、鳴らすたびに読み出しはじめる場所を変える
- **音の組み立てをフレームの外へ出した。** `sfx.*` は呼ばれた時点では予約だけして、
  実際の組み立てはそのフレームの処理が終わってから走る（マイクロタスク）。
  同じ「仕事」の中に留まるので、ブラウザの「操作があるまで鳴らせない」判定は失わない。
  ただし `resume` と戻り値を使う `toggle*` はその場で走らせる
  （AudioContext を作って動かすのは操作の直後でないと断られることがあるため）

| | 前 | 今 |
|---|---|---|
| `mg()` が輪を止める時間 | 0.34ms | **0.001ms**（予約だけ） |
| `mg()` 組み立て込み | 0.34ms | **0.11ms** |
| `explosion()` 組み立て込み | 1.40ms | **0.10ms** |
| 2機とも機銃を撃ちっぱなし | 7.6ms/秒 | **2.4ms/秒**（しかも輪の外） |

**タイマーの中も閉じた。** 息継ぎ・ノッキング・レコードの針音・曲の予約は
`setInterval`／`setTimeout` で回っている。ここで例外が出ても画面は止まらないが、
黙って壊れ続けるので、一度失敗したらそのタイマーだけを閉じて画面に出すようにした。

**この環境では Safari を再現できない**（WebKit が入っていない）。
上の4つはいずれも「音がゲームを止めうる筋道」を塞いだものだが、
まだ残っていた場合に備えて、落ちた場所が画面の帯に出るようにしてある。

### 2026-08-13: 決定とキャンセルのボタンをそろえる

画面ごとに操作が違い、パッドだけで遊ぶと**決着後に何もできなくなる**（詰む）
という報告への対処。家庭用ゲーム機の並びに合わせて、どの画面でも同じにする。

| | キーボード | パッド |
|---|---|---|
| 決定・もう一度 | Enter | **○ A**（ボタン 1） |
| 取り消し・戻る | Esc | **× B**（ボタン 0） |
| 飛行中に中断してタイトルへ | Esc | **Start**（ボタン 9） |
| プラクティスのやり直し | R | **Select**（ボタン 8） |

**○A・×B は武器と同じボタン**（20mm・7.7mm）なので、**飛んでいる間は決定・取り消しとして
読まない**。読むと、撃つたびに画面が変わってしまう。飛行中の中断は **Start** に分けた ――
武器と重ならないので、いつ押しても差し支えない。これで**終わりの無いフリープレイと
プラクティスからも、パッドだけでタイトルへ戻れる**（これまでは Esc しか出口がなかった）。

**指を離すまで受け付けない**（`src/input/PadMenu.ts`）。決着は撃ち合いの最中に出るので、
引き金を引いたままだとその一発で「もう一度」が始まってしまう。画面が変わる場面
（決着・やり直し・画面の切り替え）で受け付けを切り、**どのボタンも押されていない状態を
一度見てから**読みはじめる。ステージ選択で ×B を押してオープニングへ戻ったとき、
その押しっぱなしでまた先へ進んでしまう問題も同じ仕掛けで塞いでいる。

**受け付けは機体ごとに持つ。** 1台の判定を使い回すと、つないでいない側のパッドを
読んだ拍子に受け付けが毎フレーム切れてしまう。

**オープニングはパッドでは ○A と Start だけ**にした（キーボードは今までどおりどのキーでも可）。
どのボタンでも進む作りだと、ステージ選択から ×B で戻った直後にまた進んでしまう。
画面の文字も `PRESS ENTER KEY  OR  ○ A BUTTON` に変えた。

実測（偽のパッドを差し込んで通しで確認）: オープニングで ×B は効かず ○A で進む、
対戦中の ×B・○A は撃つだけで画面は変わらない、決着後は ○A でやり直し・×B でタイトル、
フリープレイとプラクティスは Start でタイトル、プラクティスは Select でやり直し。
○A を押しっぱなしのまま対戦へ入っても 20mm は減らない（残弾 5 のまま）。

### 2026-08-13: 地面は x で高さが変わる。街には当たり判定を持たせない

もとは一本の水平線（y = 612）だった。それだと**中央の街のところが屋根の高さの壁**になり、
街の上を飛ぶと何もない空中で墜ちる。街の中へ降りていけないのもつまらない。

**建物には当たり判定を持たせない。** 尖塔も煙突も屋根も背景の絵として通り抜ける。
地面は、その建物が立っている**谷底（道の高さ）**に合わせた。背景の絵の土の面をなぞった値
（`src/config.ts` の `GROUND`。点と点の間は直線でつなぐ）。

| x | 地面の y |
|---|---|
| 0 | 658 |
| 200 | 662 |
| 400 | 665 |
| 1000 | 665 |
| 1120 | 645 |
| 1280 | 615 |

**遠景の丘は今までどおり背景**（当たり判定なし）。稜線をなぞると左端で y = 524 になり、
飛べる空が半分になってしまう。solid なのは手前の地面だけ、という切り分けは変えていない。
右端だけは手前の丘が持ち上がるので、そこに合わせて地面も上げてある。

**実測**

| 場所 | 墜ちる高さ |
|---|---|
| 左端 x=60 | 660（656 では墜ちない） |
| 街の中央 x=650 | 666（664 では墜ちない。**前は 612**） |
| 右端 x=1240 | 626（620 では墜ちない） |

街の中央で 53px ぶん低く飛べるようになった。機体の高さは 68px なので、
地面の高さに機体の中心が来たとき車輪はちょうど道の上に乗る（絵と合わせて確認済み）。

**触った場所**: 墜落判定（対戦・プラクティス）、爆発の位置、弾が消える高さ、
コンピュータの地面回避（**行き先の x での地面**で見る）、プラクティスの輪のはみ出し確認
（安全側に `GROUND_TOP` を使う）。CPU 同士を回して**地面への自滅 0 回**、
地面までの最小の余裕は 159 / 199px（回避の余裕 150px を保てている）。

### 2026-08-13: オプション画面と設定の保存

**ステージ選択の5つ目**に「オプション」を置いた。遊び方の一覧に混ぜたのは、
別の入口（キーやボタン）を覚えてもらうより見つけやすいため。

設定は `src/settings.ts` の**一つの生きたオブジェクト**（`settings`）だけを触る。
写しを配ると「どちらが本物か」が分からなくなるため。localStorage の
`biplanes.settings` に、**変えたそのつど**保存する。

**変えたそばから効く。** 音量もフィルムも押した瞬間に変わり、「決定」を押すまで
反映されない作りは避けた。効いているかどうかを目と耳で確かめながら選べる。

| 項目 | 選べる値 |
|---|---|
| 機首の向き | 引くと上げ（既定）／ 倒すと上げ |
| スティックの遊び | 0.10 / 0.15 / 0.22 / 0.30 / 0.40 |
| 音量 | 0〜100%（10 刻み） |
| BGM | 入 / 切 |
| フィルムの強さ | 切 / 弱 / 既定 / 標準 / 強 |
| 勝ちに必要な点 | 10 / 15 / 20 / 30 / 50 |
| 7.7mm の威力 | 5 / 10 / 15（既定）/ 20 / 25 / 50 |
| 設定するパッド | 1P / 2P |
| パッドの割り当て | 7.7mm・20mm・ロール左・ロール右・全開・決定・取り消し・中断・やり直し |
| 初期設定に戻す | ― |

**「設定するパッド」から下は 1P・2P で別々に持つ**（2026-08-13 追加）。
割り当てもスティックの遊びも `[1P, 2P]` の2つ組にしてある。手元の2台が同じ機種とは限らず、
片方だけ組み替えたい場面があるため。上の共通の項目とは線を1本引いて分けてある。

- **押されたボタンは、直しているほうのパッドから読む。** 2P の割り当ては 2P のパッドで押して決める。
  その台がつながっていなければ 1台目で代用する（パッド1台でも 2P 側を組めるように）
- **入れ替えも、直しているほうのパッドの中だけ**で起こる。もう一方には触らない

**割り当ての決まりごと**

- **すでに別の操作に付いているボタンを選んだら、その操作と入れ替える。** 消して空きにすると、
  割り当てのない操作ができてしまう。入れ替えなら、どの操作にも必ずボタンが残る
- **決定と取り消しも別々に割り当てられる。** 既定は 20mm・7.7mm と同じ（○A・×B）だが、
  連動させると「20mm を L1 に移したら決定も L1 になる」という不可解なことが起きる
- **機首のスティックと十字キーは変えられない。** 番号を入れ替えても意味のある組み合わせにならない
- **待っている間はほかの操作を読まない。** 割り当てたいボタンが決定や取り消しと重なるため。
  決定に使ったボタンをそのまま拾わないよう、**一度すべて離すまで待つ**
- **8 秒で打ち切る。** パッドだけで遊んでいて押せるボタンが無くなった場合の逃げ道。
  キーボードがあれば Esc でやめられるが、無いと待ちから出られなくなる

**壊れた保存でも起動する。** 項目ごとに型と範囲を見て、外れていれば既定で埋める。
前の版の保存が残っていたり手で書き換えられていたりしても、ゲームが立ち上がらないほうが困る。
localStorage を読めない環境（プライベートモードなど）では既定のまま進む。

**7.7mm の威力（2026-08-13 追加）**は一発あたりの数字で、画面には
「15（7 発で撃墜）」と撃墜までの発数も併記する。威力だけ出しても手応えに結び付かないため。

- **整数だけを並べた。** HP に端数が出ると、計器の目盛りも「あと何発」も読みにくくなる
- **20mm は変えない。** 一撃撃墜という位置づけそのものが崩れる
- 弱くすると当てる回数が増えるぶん、**落とす前に相手のエンジンや舵を潰しきる**戦い方になる。
  性能低下は一発ごとに進むので（`PLANE.damageStep`）、5 発も当てればエンジンは下限に届く。
  これは副作用ではなく、遊び方が変わるという意味で狙いどおり
- 威力を弾に入れるのは**撃つ側**（`Bullets.fire`）。飛んでいる弾は撃った時点の威力のまま飛ぶ

**古い保存は両方に配る。** 1P・2P に分ける前の保存（`pad` / `deadzone` がひとつだけ）は、
読み込むときに両方のパッドへ同じものを入れる。設定が消えたように見えるのを防ぐ。

**実測（偽のパッドで通しで確認）**: 値の変更と保存、リロードしても残ること、
20mm を L1 に割り当てるとロール左が ○A に入れ替わること、
その状態で対戦に入ると **○A では 20mm が出ず L1 で出る**こと、
「倒すと上げ」でキーもスティックも符号が反転すること、勝ち 10 点で決着すること、
パッドだけでオプションに入って割り当てを変えて戻れること、初期設定に戻ること。

**威力の実測**: 5 → 20発、15 → 7発、25 → 4発、50 → 2発 で HP が 0 になる。

**2台つないでの実測**: 1P の 20mm を L1、2P の 20mm を △Y にすると、
**1P のパッドの L1 では 1P だけ、2P のパッドの △Y では 2P だけ**残弾が減り、
どちらの ○A でも減らない。スティックの遊びも 2P だけ 0.30 に変わる。
古い形の保存を読ませると 1P・2P の両方に同じ割り当てが入る。

### 2026-08-13: オプションは 1P・2P が同時に操作する

「1P しか操作できない」「1P / 2P を切り替える形ではなく、両方が同時にそれぞれの設定を
できるようにしたい」という要望への対応。**「設定するパッド」の切り替えは廃止**した。

- **カーソルは2本。** それぞれのパッドが自分のカーソルを動かす。1P は左から、2P は右から差す
- **人ごとに持つ項目は 1P・2P の2列**で並べ、**自分の列だけ**を書き換える。
  全体で1つの項目（音量・BGM・フィルム・勝ち点・7.7mm の威力）は、どちらが変えても同じところに効く
- **キーボードは既定で 1P の列**を担当し、**Tab で 2P の列**に移せる。
  パッドが1台しかなくても 2P 側を組めるようにするため。
  そのときは割り当ても1台目のパッドで押して決める（`pressedButtons` の代用は
  **キーボードがその列を担当しているときだけ**働く）
- **2P の列は、2台目のパッドがつながっているか、キーボードが 2P 側にいるときだけ
  カーソルを出す。** 誰も触れない列にカーソルがあっても紛らわしいだけ
- **割り当て待ちは人ごと**。二人が同時に待ちに入れる

**機首の向きも 1P・2P で別々にした**（2026-08-13）。その人の**キーボードとパッドの両方**に効く。

**画面を開き直すと壊れる不具合**（報告）も直した。原因は
**作った文字の控え（`texts`）を `create()` で空にしていなかった**こと。
Phaser は画面を作り直しても構築子を呼ばないので、`push` で貯める配列は開くたびに伸び、
2回目以降は**もう消えた文字**を掴んで色も値も動かなくなっていた。
`create()` の先頭で控えをすべて空にするようにした。
（同じ配列でも、`this.modeTexts[i] = ...` のように**添字で入れている**ところは上書きなので無事）

**もうひとつ塞いだ落とし穴**: `PadInput.read()` は「押した瞬間」を数えるので、
**1フレームに一度しか呼べない**。表示の組み立てから呼ぶと、そのぶん立ち上がりを食べてしまう。
このフレームで読んだ状態を控えて使い回すようにした。

**実測**: 3回開き直しても行数・ラベル数は 17 のまま（前は開くたびに増えていた）で、
そのあともカーソルが動く。1P と 2P が別々の行で同時に値を変えられる
（機首の向き `[false, true]`、遊び `[0.22, 0.30]`）。二人同時に割り当て待ちに入り、
1P は L1、2P は △Y をそれぞれのパッドから受け取る。パッド1台のときは 2P のカーソルが隠れ、
Tab で移すと出て、1台目のパッドで 2P の割り当てを変えられる。

### 2026-08-13: 画面端の出しろを詰める

「端から反対側へ現れるまでの余白が多く、機体が画面から消えている時間が長い」という報告への対処。

**出しろを機体1つぶん（102px）から機体の半分（51px）に**変えた（`src/objects/Plane.ts`）。
右端で機体が隠れきった瞬間に左端から出てくるので、**姿が消えている時間がなくなる**。

| | 前 | 今 |
|---|---|---|
| 一周の長さ | 1485px | 1382px |
| そのうち姿が完全に消えている距離 | **114px** | **0px** |

実機で端を3回またがせて、288 コマ中**姿が完全に消えたコマは 0**（前は巡航 288px/秒 で 0.4 秒ほど消えていた）。

### 2026-08-13: 画面をまたぐと持ち越される値（不具合）

「リプレイを繰り返しているとエンジン音だけ鳴らなくなる」という報告の原因。
**Phaser は画面を作り直しても構築子を呼ばない**ので、クラスの初期値は最初の一度しか効かない。
`create()` で戻していない値が前の試合から持ち越されていた。

- **`padWoke`（音を起こしたか）** ―― 画面を出るときにエンジン音を止めているのに、
  2回目以降は「もう起こした」ままなので**二度と鳴りはじめない**。
  BGM は `create()` から直に鳴らしているので残る ―― だから「エンジン音だけ」鳴らなくなる
- **`winner`（勝った側）** ―― 決着した状態のまま次の試合が始まり、`running` が false のままで
  **機体がまったく動かない**。こちらは報告に挙がっていなかったが、同じ原因で起きていた
- プラクティスの **`woke` / `interval` / `t`** も同じ

**直しかた**: `create()` の先頭で持ち越すと困る値をすべて戻し、
**画面に入るたびに `wakeAudio()` を呼ぶ**ようにした（音がまだ鳴らせない環境では何もしないので、
最初の操作で鳴りはじめる従来の動きは変わらない）。

**実測**: 決着 → タイトル → 対戦 を3回繰り返しても、エンジン音は 2 機ぶん鳴ったまま・
`winner` は空・機体も動く（前は 2 回目からエンジン 0・`winner` が残って機体が止まっていた）。
プラクティスも3回出入りしてエンジン音が鳴り続ける。

**同じ落とし穴を2つ潰した**（このセッション中）: オプション画面の `texts`（配列を空にし忘れ）と、
ここ。**画面をまたぐクラス変数は `create()` で戻す**、を守るところ。
`this.modeTexts[i] = ...` のように**添字で入れている**ものは上書きなので無事。

### 2026-08-13: オプションを3枚に分け、機体の性能も設定にする

項目が増えて一画面に収まらなくなったので、**入口・ゲーム設定・コントローラー設定の3枚**に分けた。
中の仕掛けは3枚とも同じで、並べる項目だけが違う（`OptionsScene` の `init({ page })`）。
どの画面も 1P・2P が同時に操作できる。

**追加した項目**

| 項目 | 選べる値 |
|---|---|
| **全開のパワー** | 150 / 180 / 220（既定）/ 260 / 300 |
| **全開で水温が上がる速さ** | 上がらない / 10.0 / 7.0 / 5.0（既定）/ 3.5 / 2.5 秒で振り切れ |
| **コンピュータの強さ** | 弱 / 普通（既定）/ 強 |

- **全開のパワーは、落ち着く速度も併記する**（`150（速度 371）`）。推力の生数字だけでは
  手応えに結び付かない。数字は `node tools/flight-probe.mjs` の実測

  | 推力 | 150 | 180 | 220 | 260 | 300 |
  |---|---|---|---|---|---|
  | 全開の速度 | 371 | 409 | **454** | 495 | 532 |

- **巡航（96・速度 288）は変えない。** 巡航まで速くすると、全開が「ここぞという時の手」でなくなる
- **水温は「振り切れまでの秒数」で見せる。** 毎秒いくつ上がるか、では読めない
- **「上がらない」を入れた**（`tempRise = 0`）。過熱そのものが起きなくなるので、
  水温の縛りが好みでない人は外せる。冷える速さ（`tempFall`）は変えない

**コンピュータの強さは、ステージ選択の左右と同じもの**（2026-08-13 追加）。
どちらで変えても `settings.aiLevel` を見る ―― 画面ごとに持つと、
どちらが本当の設定か分からなくなる。あわせて**保存されるようになった**
（これまでは画面の中だけの値で、読み込み直すと普通に戻っていた）。
ステージ選択では、腕前を使わない遊び方を選んでいるときは薄く表示する ―― これは前からの動き。

**「古いフィルム風の効果」は前からあった**（「フィルムの強さ」という名前）。
呼び名が伝わっていなかったので、絵の見た目そのままの名前に変えた。

**設定はモデルの中で読む。** 全開の推力は `stepFlight`（`src/flight.ts`）、
水温の上がり方は `Plane.updateTemp` が `settings` を直に見る。
`tools/flight-probe.mjs` は `src/flight.ts` を束ねて Node で回すが、
`settings` は既定値のまま立ち上がるので、プローブの数字は今までどおり既定の性能を測る。

**実測**: 推力 150 → 速度 377、220 → 457、300 → 534（画面に出す 371 / 454 / 532 は
自然に落ち着かせたときの値で、こちらは高度を押さえて測ったぶん少し高い）。
「上がらない」では 6 秒吹かし続けても水温 0.30 のまま。2.5 秒設定では振り切れて
エンジンが傷み、速度が 457 → 419 に落ちる（過熱が効いている）。

**ついでに直したもの**: 一覧の外を指す添字で `change` / `decide` を呼ぶと
`undefined.kind` で落ちていた。画面の作りでは起きないが、落ちると画面ごと死ぬので受け止めるようにした。

### 2026-08-15: 全体点検で見つかった不具合と、作りそのものの手直し

コードを通しで見直して見つかったものを、まとめて直した。

#### 直した不具合

| | 症状 | 原因 | 直しかた |
|---|---|---|---|
| **A-1** | オプションで選んだ**フィルムの強さが試合に出ない** | 掛け直すときに前の画面のパイプラインの強さを引き継いでいた（画面を作り直しても構築子は呼ばれないので、`this.film` が古いまま残る） | 強さは**必ず `settings.film` から取る**。1-5 キーも設定に書き込むので、掛け直しでも強さは保たれる |
| **A-2** | **「BGM 切」で保存しても、立ち上げると鳴る** | 入切の写しを class の初期値で持っていた。写しを作る時期が、保存を読む時期（`loadSettings()`）より早い | 写しをやめ、**`settings.bgm` ひとつだけ**を見る |
| **A-3** | **「決定」「取り消し」を割り当て直すと暴発する**（決定→△Y で待ちから抜けられない、取り消し→□X で画面から蹴り出される） | 決めたボタンはまだ押されたまま。立ち上がりを見ている側は割り当てが変わる前の別のボタンを見張っていたので、押しっぱなしでも「今押された」に見える | 割り当てを決めたら、**指を離すまでその人の操作を読まない**（`settling`） |
| **A-4** | **速い弾がすり抜ける** | 当たり判定がその瞬間の位置だけを見る点判定だった。7.7mm は 60fps でも1フレームに 25px 進む | **前のフレームと今を結んだ線分**で見る（`src/collision.ts`）。相手も動くので、線分は相手から見た動きで作る |
| **A-5** | **保存の検査がゆるい**（`aiLevel: 4` で 2P が人になる、`mgDamage: 12.5` で HP に端数） | 検査が「1以上」「100以下」のような範囲だけで、画面が並べる値と別々に書かれていた | **画面が並べるのも検査するのも同じ表**（`settings.ts` の `CHOICES`）。表から外れた値はいちばん近いものへ寄せる |

**A-4 の実測**（判定の中を通った弾のうち、実際に当たった割合）

| | 静止した相手 | 正面から突っ込む相手 |
|---|---|---|
| 60fps | 98% → **100%** | 96% → **100%** |
| 30fps | 90% → **100%** | 81% → **100%** |
| 20fps（`dt` の上限） | 71% → **100%** | 54% → **100%** |

#### あわせて直したもの

- **弾も画面端で折り返す。** 機体だけが向こう側から出てきて弾は端で消える、という食い違いをなくした。
  折り返す位置は機体と同じ（`WRAP.pad` = 機体の半分）
- **コンピュータが画面端をまたいで狙う。** 端の向こうにいる相手を「画面の反対側にいる遠い相手」と
  見て、わざわざ長いほうを回っていた。近いほうの差で見る（`shortestDx`）
- **読み込み中の画面を出す。** 絵は合わせて 9MB ほどあり、それまでは真っ暗な画面が続いていた。
  読めなかった絵があれば、その旨を画面に出す（読めた絵だけで遊べるので止めはしない）
- **オプションで、全体で1つの項目に 2P のカーソルが乗ったときも値が光る。** 列が1つしかないため、
  2P が選んでいるときだけ何も光らなかった
- **相手が割り当て中は、こちらの取り消しで画面を閉じない。** 二人で同時に触れる画面なので、
  片方の取り消しでもう片方の作業が消えるのは理不尽

#### 作りそのもの

- **`create()` で戻すものを一か所にまとめた**（`PlayScene.resetState()`）。
  画面は作り直されても構築子は呼ばれないので、戻し忘れると症状が「2回目から」出て原因が見えにくい。
  このセッションまでに同じ落とし穴を4つ踏んでいる（`texts` / `padWoke` / `winner` / `film`）
- **オプションの項目を `src/ui/optionRows.ts` へ出した。** 「何が並ぶか」と「どう描くか」を分ける
- **全開のパワーの「速度」は表を書き写さず、飛びの計算から出す**（`levelSpeed`）。
  書き写すと、飛びの数値をいじったときに表示だけが古いまま残る
- **`tools/flight-probe.mjs` に `--thrust=` を足した。** 全開のパワーはオプションで変えられるので、
  既定値だけを測っても遊ぶ人が触る範囲が分からない
- **`src/audio.ts`（991行）を分けた** ―― 素と入れ物（`src/sound/tone.ts`）、
  エンジン音（`engine.ts`）、曲（`bgm.ts`）、まとめ役（`audio.ts`）
- **コンピュータの腕前の表を `src/config.ts` へ移した。** 保存の検査が「段階がいくつあるか」を
  そこから取れるようにするため。別々に持つと、段階を増やしたときに検査だけが古いまま残る

#### 直さなかったもの

- **機体どうしの当たり判定は無いまま。** 本家にも無く、すれ違いざまの読み合いが濁るため
- **得点の表示は3桁で頭打ち。** 勝ちに必要な点は最大 50 なので対戦では届かない。
  計器は3桁の機械式カウンタとして描いてあるので、桁は増やさない

### 2026-08-15: ＋で一時停止、過熱の音、パッドの振動、割り当ての直押し

#### ＋（Start）と Esc は、いったん止めて聞き直す

押した瞬間にタイトルへ戻る作りだったので、飛んでいる最中に押し間違えると
試合がそこで消えていた。**一時停止して「ゲームを終了しますか？」を出す**（`src/ui/PauseMenu.ts`）。

- 止めている間は**何も進めない**。機体・弾・気球・煙・開始の合図まで止まる
- **カーソルの初めの位置は「いいえ、つづける」**。押し間違えたときに、
  もう一度＋を押すだけで戻れる（＋・×B・Esc のどれでも戻れる）
- エンジン音は絞り、曲は小さくして残す ―― 止めると戻ったときに頭から鳴り直すため
- **戻るときは「GO!」を 0.5 秒**出してから動きだす（`Countdown.beginResume()`）。
  いきなり動くと握り直す間がない。開始の合図と違って**出撃位置には戻さない**
- 1P・2P のどちらのパッドでも操作できる。対戦・フリープレイ・プラクティスで同じ
- **決着が付いたあと**は、画面に戻り方が出ているのでそのままタイトルへ戻す（確認を挟まない）

#### 過熱の音は「息切れ」（案B）

赤帯に入った瞬間だけ変わって、あとは振り切れるまで同じだった ――
**入切の2値だったのが理由**。900Hz より上が占める割合は、赤帯 0.358 → 振り切れ 0.365 で
ほとんど動いていなかった。

**赤帯から振り切れまでを 0〜1 として、踏み込むほど悪くなる**ように変えた（`Plane.strain`）。
鳴らし方は試作台（`samples/overheat-sound-sample.html`）で3案を聞き比べて**案B「息切れ」を採用**:

| | 中身 |
|---|---|
| **失火** | 燃え損ねて音が飛ぶ。踏み込むほど頻繁につんのめる（0.12 + 0.55×踏み込み の割合で、190ms ごとに判定） |
| **ノッキング** | 金属を叩く音に細い余韻を添える。**回転（プロペラの刻み）の何発かに一度**打ち、踏み込むほど間隔が詰まる |
| **うなり** | 回転が不揃いになる（音程の揺れ 26〜84 セント） |
| **蒸気** | うっすらとした噴き出し。上の3つを支えるだけの薄さ |

**ノッキングを等間隔にしない**のが効いている。130ms ごとの等間隔では節に聞こえて、
機械が痛んでいる感じにならなかった。実測では振り切れで音量が 0.17 → 0.12 に落ちる
（＝音が飛んでいる）ぶん、つんのめりが耳で分かる。

#### パッドの振動

`vibrationActuator.playEffect('dual-rumble', …)` を使う。Chrome・Edge で動き、
**Safari は非対応、Firefox は機種による。使えない環境では黙って何もしない**。

| 出来事 | 高い錘 | 低い錘 | 長さ | 順位 |
|---|---|---|---|---|
| 7.7mm を受けた | 0.45 | 0.15 | 110ms | 1 |
| 水温が赤帯 | 0.30 | 0.25 | 260ms を 240ms ごと | 0 |
| 撃墜された | 0.65 | **1.00** | 420ms | 2 |

- **弾は高い錘、爆発は低い錘**を効かせる。同じ強さでも別のものとして手に伝わる
- **順位を持たせる**。`playEffect` は後から頼んだものが前を打ち切るので、
  順位が無いと撃墜の手応えが直後の細かい振動で消える
- 振動は**当たった本人のパッドだけ**へ送る（`PadInput.gamepadIndex` ―― 読んでいるのと同じパッド）。
  コンピュータが操縦している機の分は送らない
- 赤帯のうなりは**踏み込み具合で強くなる**（0.5〜1.0 倍）
- 強さは**1P・2P それぞれ**設定できる: 切 / 弱 / 標準（既定）/ 強。選ぶとその場で試しに震える

#### ボタンの割り当ては直押し

「決定してから押す」の二段構えをやめ、**割り当てたい行で、そのボタンを押せば入る**。
待ちの状態そのものが要らなくなった。

- 拾うのは**押した瞬間**だけ。押しっぱなしのまま項目を移っても、移った先は変わらない
- **十字キーの上下は割り当てに使えない**（項目を選ぶのに要る）
- 割り当てた直後は、指を離すまでその人の操作を読まない ――
  決定や取り消しを割り当て直したとき、その押しっぱなしがそのまま効いてしまうのを断つ
- パッドが1台しかなく、キーボードが 2P 側を借りている間は、1P の列を触らせない
  （1台のパッドで両方の列に同じボタンが入ってしまうため）

#### 重ねられるボタンの判別を直した

**同じ場面で読む操作どうしだけがぶつかる**ようにした（`padConflicts`）。

|  | いつ読むか |
|---|---|
| 7.7mm・20mm・ロール左右・全開 | 飛んでいる間 |
| 決定・取り消し | 画面の選択 |
| 中断・やり直し | どちらでも |

すべてを一つの集まりとして見ていたため、**決定に○A を割り当てると 20mm がそこから追い出されて**いた。
この二つは同じ場面に出てこない（飛んでいる間は決定を読まず、画面では 20mm を撃たない）ので、
追い出す理由がない。既定が「20mm と決定が同じ○A」なのも同じ理由。
