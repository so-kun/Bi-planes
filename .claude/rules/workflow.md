# 進め方のルール

## フェーズ

1. ~~**ディスカッション期**: ゲームシステム / UI・キャラデザイン / 効果音・BGM の3点を、
   「提案 → サンプル提示 → ユーザー採用」の順で固める~~ → 完了
2. ~~**実装第1弾（M1）**: 1P のみ操縦できるプレイテスト版（気球・的だけの空）~~ → 完了
3. ~~**実装第2弾（M2）**: 2P対戦・スコア・勝敗~~ → 完了（2026-08-12）
4. **仕上げ（M3・現在）**: 2人でのプレイテストを踏まえたバランス調整、
   タイトル画面、演出の強化。

新しい要素を足すときは、これまでどおり「提案 → サンプル提示 → ユーザー採用」の順で進める。
`docs/decisions.md` の「検討中」にあるものは、採用されるまで実装しない。

## Git 運用

- ベースブランチは `main`。作業ブランチを切って PR を作成して進める
- **区切りがついた時点で main にマージする**（2026-08-12 決定）。
  PR を長く開いたままにせず、レビューが済んだらその都度 main に取り込む。
  main が常に「動く最新版」になっている状態を保つ
- main へ直接 push はしない。必ず作業ブランチ → PR → マージの順で進める
- マージ後は作業ブランチを main から切り直してから次の作業を始める
- コミットメッセージは日本語または英語で、変更の意図がわかるように書く
- サンプル類は `samples/` に置く（ゲーム本体のコードとは分離する）

## 記録

- 決定事項は `docs/decisions.md` に追記（決定日・内容・理由）。未決事項は「検討中」として同ファイルに列挙
- 各セッションの経過は `docs/progress.md` に追記（日付・やったこと・次にやること）
- ゲーム仕様の詳細は `docs/game-design.md` に集約し、決定に合わせて更新する
